# aliases for GRC from homebrew
# [ `uname` = "Darwin" && -d /usr/local/etc/grc.bashrc ] && source /usr/local/etc/grc.bashrc
# [ `uname` = "Darwin" && -d /opt/homebrew/etc/grc.bashrc ] && source /opt/homebrew/etc/grc.bashrc
